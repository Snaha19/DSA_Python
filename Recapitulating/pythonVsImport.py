# python file.py is used to execute a Python file directly from the terminal, 
# whereas import file is used to include another Python file as a module inside a program. 
# Writing import file.py gives an error because Python does not allow file extensions in import statements.git add .
git add .
git commit -m "added q3 pattern problem"
git push